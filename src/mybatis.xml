<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 定义类型别名 -->
	<typeAliases>
	   <!-- <typeAlias alias="Student" type="com.situ.mybatis.entity.Student"/>
	   		<typeAlias alias="Banji" type="com.situ.mybatis.entity.Banji"/> -->
	   <!-- 扫描包里面的类，批量起别名，别名即为类名，别名不区分大小写 -->
	   <!-- 别名就是让resultType的路径可以简写 -->
	   <package name="com.situ.ssm.entity"/>
	   <package name="com.situ.ssm.vo"/>
	   <!-- <package name="其他包"/> -->
	</typeAliases>

	<environments default="development">
     <environment id="development">
        <!-- 使用JDBC的事物管理 -->
        <transactionManager type="JDBC"/>
        <!-- 配置数据库连接池 -->
        <dataSource type="POOLED">
          <property name="driver" value="com.mysql.jdbc.Driver"/>
          <property name="url" value="jdbc:mysql://localhost:3306/java180703?characterEncoding=UTF-8"/>
          <property name="username" value="root"/>
          <property name="password" value="root"/>
        </dataSource>
     </environment>
	</environments>
   
   <!-- 加载映射文件 -->
   <mappers>
     <mapper resource="com/situ/ssm/entity/StudentMapper.xml"/>
     <mapper resource="com/situ/ssm/entity/BanjiMapper.xml"/>
   </mappers>
</configuration>